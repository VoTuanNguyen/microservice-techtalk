server:
  port: 8888
  
spring:  cloud:    config:      server:        git:          uri: https://github.com/VoTuanNguyen/microservice-config-repo
          # username:
          # password:
          search-paths:          - "configuration"  application:    name: config-server
eureka:
  client:
    eureka-server-port: 8761